version: '3.8'

services:
  rabbitmq:
    image: rabbitmq:3.13-management-alpine
    container_name: rabbitmq-test-prod-native
    ports:
      - "5672:5672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    volumes:
      - rabbitmq_prod_native_data:/var/lib/rabbitmq
    networks:
      - prod-native-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: ../../..
      dockerfile: docker/prod/native/Dockerfile
    container_name: rabbitmq-test-app-prod-native
    ports:
      - "8080:8080"
    environment:
      - QUARKUS_RABBITMQ_HOST=rabbitmq
      - QUARKUS_RABBITMQ_PORT=5672
      - QUARKUS_RABBITMQ_USERNAME=guest
      - QUARKUS_RABBITMQ_PASSWORD=guest
    depends_on:
      rabbitmq:
        condition: service_healthy
    networks:
      - prod-native-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/q/health"]
      interval: 30s
      timeout: 10s
      retries: 3

networks:
  prod-native-network:
    driver: bridge

volumes:
  rabbitmq_prod_native_data: